<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE xml> <!-- Just to suppress the warning: No grammar constraints (DTD or XML Schema) referenced in the document -->
<!-- https://logging.apache.org/log4j/2.x/manual/configuration.html -->
<!-- Configuration status="ERROR". When this is present log4j2 will output any errors associated with incorrect configuration at runtime. -->
<Configuration status="ERROR">
  <Appenders>
    <Console name="Console" target="SYSTEM_OUT">
      <PatternLayout pattern="%d{HH:mm:ss.SSS} [%t] %-5level %logger{36} - %msg%n"/>
    </Console>
    <File name="VitAppLog" filename="VitApp.log" append="false">
      <PatternLayout pattern="%d{HH:mm:ss.SSS} [%t] %-5level %class{36} %L %M - %msg%xEx%n"/>
    </File> 
  </Appenders>
  <Loggers>
	<!-- Log all SQL DML statements as they are executed -->
	<Logger name="org.hibernate.SQL" level="error" />
	<!-- Log all JDBC parameters -->
	<Logger name="org.hibernate.type" level="error" />
	<!-- Log all SQL DDL statements as they are executed -->
	<Logger name="org.hibernate.tool.hbm2ddl" level="error" />
	<!-- Log the state of all entities (max 20 entities) associated with the session at flush time -->
	<Logger name="org.hibernate.pretty" level="error" />
	<!-- Log all second-level cache activity -->
	<Logger name="org.hibernate.cache" level="error" />
	<!-- Log transaction related activity -->
	<Logger name="org.hibernate.transaction" level="error" />
	<!-- Log all JDBC resource acquisition -->
	<Logger name="org.hibernate.jdbc" level="error" />
	<!-- Log HQL and SQL ASTs during query parsing -->
	<Logger name="org.hibernate.hql.ast.AST" level="error" />
	<!-- Log all JAAS authorization requests -->
	<Logger name="org.hibernate.secure" level="error" />
	<!-- Log everything (a lot of information, but very useful for troubleshooting) -->
	<Logger name="org.hibernate" level="error" />
    <Root level="info"> <!-- "trace", "debug", "info", "warn", "error" -->
      <AppenderRef ref="Console"/>
      <AppenderRef ref="VitAppLog"/>
    </Root>
  </Loggers>
</Configuration>